# Quita aí — variáveis de ambiente
# Atualmente nenhuma variável é necessária (app usa localStorage).
# Quando migrar para Supabase, descomente as linhas abaixo:

# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
